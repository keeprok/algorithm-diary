# ✈️ 여행 읽기용 · 2Lv 유형 드릴 (9/21~9/28)

> **목적**: 제약조건(N 크기) 보고 **유형 맞히기** 드릴.
> 답을 외우는 게 아니라 "N이 10만이면 O(N log N)" 판단을 빠르게.
>
> **읽는 법**: `제약조건` 먼저 보고 → 머릿속으로 유형 추측 → `유형`/`한 줄 접근법` 확인.

---

## 📏 제약조건 → 복잡도 감각 (제일 중요)

| N 크기 | 허용 복잡도 | 대표 유형 |
|---|---|---|
| N ≤ 10~12 | O(N!) | 순열 완전탐색 |
| N ≤ 20 | O(2^N) | 부분집합/DFS 완전탐색 |
| N ≤ 100~500 | O(N³) | 3중 반복, 플로이드 |
| N ≤ 5,000 | O(N²) | 2중 반복 |
| N ≤ 100,000 | O(N log N) | 정렬, 힙, 이분탐색 |
| N ≤ 10,000,000 | O(N) | 해시, 투포인터, 그리디 |
| N ≥ 10억 | O(log N) | **답을 이분탐색** |

---

## 🎯 15문제 드릴 (유형 분산)

| # | 문제명 | 제약(N) | 핵심 키워드 | 유형 | 한 줄 접근법 (왜 이 유형?) |
|---|---|---|---|---|---|
| 1 | 전화번호 목록 | ≤ 100만 | 접두사 | 해시/정렬 | 정렬하면 접두사는 **바로 옆**에만 생김 |
| 2 | 의상 | 종류 ≤ 30 | 조합 경우의수 | 해시 | 종류별 (개수+1) 곱 − 1 (안 입기 포함) |
| 3 | 가장 큰 수 | ≤ 10만 | 문자열 이어붙여 비교 | 정렬 | 정렬 **기준만** 커스텀(`b+a` vs `a+b`) |
| 4 | H-Index | 논문 ≤ 1,000 | 내림차순 인덱스 비교 | 정렬 | N 작아서 정렬 후 훑기로 충분 |
| 5 | 게임 맵 최단거리 | 100×100 | 최단거리 | BFS | 가중치 없는 최단거리 = **무조건 BFS** |
| 6 | 네트워크 | n ≤ 200 | 연결 요소 | DFS/BFS | 서로 이어진 덩어리 **개수** 세기 |
| 7 | 타겟 넘버 | 수 ≤ 20 | +/− 조합 | 완전탐색/DFS | 2^20 ≈ 100만 → 전부 탐색 OK |
| 8 | 단어 변환 | words ≤ 50 | 한 글자씩 변환 | BFS | **최소** 변환 횟수 = 최단경로 |
| 9 | 큰 수 만들기 | ≤ 100만, k제거 | 앞자리 크게 유지 | 스택(그리디) | 뒤가 더 크면 앞을 pop = 모노토닉 스택 |
| 10 | 주식가격 | ≤ 10만 | 안 떨어진 기간 | 스택 | 값 떨어지는 순간 스택에서 정산 |
| 11 | 더 맵게 | ≤ 100만 | 최솟값 반복 추출 | 힙 | 매번 최소 2개 꺼냄 → 최소힙 O(log N) |
| 12 | 구명보트 | ≤ 5만 | 가벼운+무거운 짝 | 투포인터 | 정렬 후 **양 끝**에서 좁혀오기 |
| 13 | 두 큐 합 같게 | ≤ 30만 | 두 합 맞추기 | 투포인터 | 두 큐 이어 슬라이딩, 반복 상한 주의 |
| 14 | 피로도 | dungeons ≤ 8 | 던전 순서 | 완전탐색 | 8! = 40320 → 모든 순서 시도 |
| 15 | 입국심사 | n ≤ 10억 | 시간 이분탐색 | 이분탐색 | **답 자체를 이분** (파라메트릭 서치) |

> 💡 이미 푼 것(1~13 대부분)은 "왜 그 유형이었나" 복기용,
> 안 푼 것(14 피로도 · 15 입국심사)은 대표 유형 눈도장용.

---

## 📚 고득점 Kit — 개념별 지도

각 카테고리에서 **꼭 알아야 할 대표 문제 + 판별 신호**.
`[✓]` = 이미 풀어봄.

### 1. 해시 (Hash) — "빠른 조회/집계"
- **신호**: 개수 세기, 존재 여부, 짝 찾기, "몇 종류"
- **도구**: `Map`(개수), `Set`(존재)
- 문제: 완주하지 못한 선수 · 폰켓몬 · 전화번호목록[✓] · 의상[✓] · 베스트앨범[✓]

### 2. 스택/큐 (Stack/Queue) — "순서 있는 처리"
- **신호(스택)**: 짝 맞추기(괄호), "직전 것과 비교", 되돌리기
- **신호(큐)**: 먼저 온 것 먼저, 시뮬레이션
- 문제: 같은 숫자는 싫어 · 기능개발[✓] · 올바른 괄호[✓] · 프로세스[✓] · 다리를 지나는 트럭[✓] · 주식가격[✓]

### 3. 힙 (Heap) — "매번 최대/최소 꺼내기"
- **신호**: "가장 작은/큰 것을 반복해서" 꺼내야 함
- **왜?**: 매번 정렬하면 느림 → 힙은 추출 O(log N)
- 문제: 더 맵게[✓] · 디스크 컨트롤러[✓] · 이중우선순위큐[✓]

### 4. 정렬 (Sort) — "기준을 잘 잡으면 끝"
- **신호**: "가장 큰/작은 배치", 순위, 커스텀 순서
- **핵심**: `sort` **비교 함수**를 뭘로 잡느냐가 전부
- 문제: K번째수 · 가장 큰 수[✓] · H-Index[✓]

### 5. 완전탐색 (Brute Force) — "N 작으면 다 해봐"
- **신호**: N ≤ 20, "모든 경우", 순열/조합
- **도구**: 재귀 DFS, 비트마스크, 순열
- 문제: 최소직사각형 · 모의고사 · 소수찾기[✓] · 카펫[✓] · **피로도** · 모음사전

### 6. 탐욕법 (Greedy) — "매 순간 최선"
- **신호**: "최소 횟수/최대 이익", 정렬 후 순서대로
- **주의**: 반례 없는지 항상 의심
- 문제: 체육복 · 조이스틱[✓] · 큰 수 만들기[✓] · 구명보트[✓] · 섬 연결하기 · 단속카메라

### 7. DP (동적계획법) — "작은 답으로 큰 답"
- **신호**: "경우의 수", "최댓값/최솟값"인데 완탐 하면 터짐, 겹치는 부분문제
- **핵심**: 점화식 `dp[n] = f(dp[n-1]...)`
- 문제: 멀리뛰기[✓] · 피보나치[✓] · 2×n타일링[✓] · N으로 표현 · 정수삼각형 · 등굣길 · 도둑질

### 8. DFS/BFS — "그래프/격자 탐색" ⭐아래 상세
- 문제: 타겟넘버[✓] · 네트워크[✓] · 게임맵최단거리[✓] · 단어변환[✓] · 여행경로[✓] · 아이템줍기 · 무인도여행[✓]

### 9. 이분탐색 (Binary Search) — "답을 반씩 좁히기"
- **신호**: 정렬된 데이터에서 찾기 / N이 **10억** / "최소의 최대" "최대의 최소"
- **핵심**: 값을 직접 계산 못 하면 **답을 가정하고 검증**(파라메트릭)
- 문제: **입국심사** · 징검다리

---

## ⭐ DFS / BFS 완전 이해 (여행 핵심)

> 갔다 와서 2Lv 그래프 문제 바로 풀 수 있게, 이 섹션만은 확실히.

### 🧠 한 줄 정의
- **DFS(깊이 우선)**: 한 길로 **끝까지** 갔다가 막히면 되돌아옴 → 재귀/스택
- **BFS(너비 우선)**: **가까운 곳부터** 물결처럼 퍼짐 → 큐

### 🎯 뭘 쓸지 3초 판별
| 상황 | 선택 | 이유 |
|---|---|---|
| **최단 거리/최소 횟수** | **BFS** | 먼저 도달 = 최단 (가중치 없을 때) |
| 모든 경우/조합/순열 | DFS | 끝까지 파고들며 경우 생성 |
| 연결 요소 개수 | 아무거나 | 둘 다 덩어리 다 훑음 |
| 백트래킹(되돌리기) | DFS | 재귀가 자연스러움 |
| 사이클/경로 존재 | DFS | 깊이 탐색이 편함 |

> **가장 중요**: "**최단**"이란 단어 나오면 → **BFS** 반사적으로!

### 🧩 격자(2D) BFS 템플릿
```javascript
function bfs(maps) {
  const n = maps.length, m = maps[0].length;
  const dx = [-1, 1, 0, 0];   // 상하좌우
  const dy = [0, 0, -1, 1];
  const queue = [[0, 0, 1]];  // [행, 열, 거리]
  maps[0][0] = 0;             // 방문 처리 (큐 넣을 때 바로!)

  while (queue.length) {
    const [r, c, dist] = queue.shift();
    if (r === n-1 && c === m-1) return dist;  // 도착 = 최단

    for (let d = 0; d < 4; d++) {
      const nr = r + dx[d], nc = c + dy[d];
      if (nr < 0 || nr >= n || nc < 0 || nc >= m) continue; // 범위
      if (maps[nr][nc] !== 1) continue;                     // 벽/방문
      maps[nr][nc] = 0;              // 방문 처리 (중복 방지 핵심!)
      queue.push([nr, nc, dist + 1]);
    }
  }
  return -1;
}
```
**포인트**: 방문 처리는 **큐에 넣는 순간**. (꺼낼 때 하면 중복으로 터짐)

### 🧩 격자(2D) DFS 템플릿 — 연결 요소
```javascript
function dfs(grid, r, c, visited) {
  const n = grid.length, m = grid[0].length;
  if (r < 0 || r >= n || c < 0 || c >= m) return; // 범위
  if (visited[r][c] || grid[r][c] === 'X') return; // 방문/벽
  visited[r][c] = true;

  dfs(grid, r-1, c, visited);
  dfs(grid, r+1, c, visited);
  dfs(grid, r, c-1, visited);
  dfs(grid, r, c+1, visited);
}
// 바깥 이중 for에서 방문 안 한 칸 만날 때마다 dfs 호출 → 호출 횟수 = 덩어리 수
```

### 🧩 그래프(인접리스트) 탐색
```javascript
// 간선 목록 → 인접 리스트
const graph = Array.from({length: n+1}, () => []);
for (const [a, b] of edges) {
  graph[a].push(b);
  graph[b].push(a); // 양방향이면
}
// 이후 위 템플릿과 동일하게 visited 관리하며 이웃 순회
```

### 🧩 백트래킹(순열/조합) DFS
```javascript
function dfs(current, used) {
  if (current.length === target) { /* 정답 처리 */ return; }
  for (let i = 0; i < arr.length; i++) {
    if (used[i]) continue;
    used[i] = true;
    dfs([...current, arr[i]], used);
    used[i] = false; // 되돌리기 = 백트래킹
  }
}
```

### ✅ 실수 체크리스트
- [ ] **방문 처리** 했는가? (안 하면 무한루프/시간초과)
- [ ] BFS 방문 처리를 **큐 넣을 때** 했는가? (꺼낼 때 ❌)
- [ ] 범위 체크 `0 <= x < n` 넣었는가?
- [ ] "최단"인데 DFS 쓰고 있진 않은가? → BFS로
- [ ] 그래프 **양방향**인가 단방향인가?

---

## 🗺️ 돌아온 후 루틴 (9/29~)
1. 이 표 다시 보며 **유형 먼저 맞히기** (5분)
2. **DFS/BFS 미풀이** 도전: 아이템 줍기 · 여행경로 복습
3. **완전탐색**: 피로도 · 모음사전
4. **이분탐색**: 입국심사 (파라메트릭 서치 첫 도전)
5. 하루 2문제, 주석 먼저 → 코드

**화이팅! 폰으로 눈에 익히고 오면 절반은 먹고 들어가요 💪**

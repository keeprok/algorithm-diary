function solution(A, B) {
  // A는 오름차순, B는 내림차순으로 정렬
  // 가장 작은 수 × 가장 큰 수 → 곱의 합이 최소가 됨
  A.sort((a, b) => a - b);
  B.sort((a, b) => b - a);

  // 같은 인덱스끼리 곱해서 합산
  return A.reduce((sum, a, i) => sum + a * B[i], 0);
}
// 1. 한 배열은 오름차순, 다른 배열은 내림차순으로 정렬
// 2. 같은 인덱스끼리 곱의 합 → 수학적으로 이 방법이 최솟값을 보장

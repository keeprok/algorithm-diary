function solution(n) {
  // x는 2 이상이어야 하고, n % x === 1을 만족하는 가장 작은 x 반환
  for (let x = 2; x < n; x++) {
    if (n % x === 1) return x;
  }
}
// 1. x는 최소 2부터 시작 (1이면 나머지가 0이 되므로 제외)
// 2. 작은 수부터 순서대로 탐색하므로 처음 만족하는 x가 정답
